/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.icepanel.api.resources.drafts;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.icepanel.api.core.ClientOptions;
import com.icepanel.api.core.IcePanelClientApiException;
import com.icepanel.api.core.IcePanelClientException;
import com.icepanel.api.core.IcePanelClientHttpResponse;
import com.icepanel.api.core.MediaTypes;
import com.icepanel.api.core.ObjectMappers;
import com.icepanel.api.core.QueryStringMapper;
import com.icepanel.api.core.RequestOptions;
import com.icepanel.api.errors.BadRequestError;
import com.icepanel.api.errors.ConflictError;
import com.icepanel.api.errors.ForbiddenError;
import com.icepanel.api.errors.InternalServerError;
import com.icepanel.api.errors.NotFoundError;
import com.icepanel.api.errors.TooManyRequestsError;
import com.icepanel.api.errors.UnauthorizedError;
import com.icepanel.api.errors.UnprocessableEntityError;
import com.icepanel.api.resources.drafts.requests.DraftCreateRequest;
import com.icepanel.api.resources.drafts.requests.DraftDeleteRequest;
import com.icepanel.api.resources.drafts.requests.DraftFindRequest;
import com.icepanel.api.resources.drafts.requests.DraftMergeRequest;
import com.icepanel.api.resources.drafts.requests.DraftUpdateRequest;
import com.icepanel.api.resources.drafts.requests.DraftUpsertRequest;
import com.icepanel.api.resources.drafts.requests.DraftsListRequest;
import com.icepanel.api.resources.drafts.types.DraftsCreateResponse;
import com.icepanel.api.resources.drafts.types.DraftsGetResponse;
import com.icepanel.api.resources.drafts.types.DraftsListResponse;
import com.icepanel.api.resources.drafts.types.DraftsMergeResponse;
import com.icepanel.api.resources.drafts.types.DraftsUpdateResponse;
import com.icepanel.api.resources.drafts.types.DraftsUpsertResponse;
import com.icepanel.api.types.Error;
import java.io.IOException;
import java.util.Map;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawDraftsClient {
    protected final ClientOptions clientOptions;

    public RawDraftsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public IcePanelClientHttpResponse<DraftsListResponse> list(String landscapeId, String versionId) {
        return list(landscapeId, versionId, DraftsListRequest.builder().build());
    }

    public IcePanelClientHttpResponse<DraftsListResponse> list(
            String landscapeId, String versionId, DraftsListRequest request) {
        return list(landscapeId, versionId, request, null);
    }

    public IcePanelClientHttpResponse<DraftsListResponse> list(
            String landscapeId, String versionId, DraftsListRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("landscapes")
                .addPathSegment(landscapeId)
                .addPathSegments("versions")
                .addPathSegment(versionId)
                .addPathSegments("drafts");
        if (request.getFilter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter", request.getFilter().get(), false);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new IcePanelClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DraftsListResponse.class), response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new IcePanelClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new IcePanelClientException("Network error executing HTTP request", e);
        }
    }

    public IcePanelClientHttpResponse<DraftsCreateResponse> create(
            String landscapeId, String versionId, DraftCreateRequest request) {
        return create(landscapeId, versionId, request, null);
    }

    public IcePanelClientHttpResponse<DraftsCreateResponse> create(
            String landscapeId, String versionId, DraftCreateRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("landscapes")
                .addPathSegment(landscapeId)
                .addPathSegments("versions")
                .addPathSegment(versionId)
                .addPathSegments("drafts")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new IcePanelClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new IcePanelClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DraftsCreateResponse.class), response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new IcePanelClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new IcePanelClientException("Network error executing HTTP request", e);
        }
    }

    public IcePanelClientHttpResponse<DraftsGetResponse> get(String landscapeId, String versionId, String draftId) {
        return get(landscapeId, versionId, draftId, DraftFindRequest.builder().build());
    }

    public IcePanelClientHttpResponse<DraftsGetResponse> get(
            String landscapeId, String versionId, String draftId, DraftFindRequest request) {
        return get(landscapeId, versionId, draftId, request, null);
    }

    public IcePanelClientHttpResponse<DraftsGetResponse> get(
            String landscapeId,
            String versionId,
            String draftId,
            DraftFindRequest request,
            RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("landscapes")
                .addPathSegment(landscapeId)
                .addPathSegments("versions")
                .addPathSegment(versionId)
                .addPathSegments("drafts")
                .addPathSegment(draftId)
                .build();
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new IcePanelClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DraftsGetResponse.class), response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new IcePanelClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new IcePanelClientException("Network error executing HTTP request", e);
        }
    }

    public IcePanelClientHttpResponse<DraftsUpsertResponse> upsert(
            String landscapeId, String versionId, String draftId, DraftUpsertRequest request) {
        return upsert(landscapeId, versionId, draftId, request, null);
    }

    public IcePanelClientHttpResponse<DraftsUpsertResponse> upsert(
            String landscapeId,
            String versionId,
            String draftId,
            DraftUpsertRequest request,
            RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("landscapes")
                .addPathSegment(landscapeId)
                .addPathSegments("versions")
                .addPathSegment(versionId)
                .addPathSegments("drafts")
                .addPathSegment(draftId)
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new IcePanelClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new IcePanelClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DraftsUpsertResponse.class), response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new IcePanelClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new IcePanelClientException("Network error executing HTTP request", e);
        }
    }

    public IcePanelClientHttpResponse<Map<String, Object>> delete(
            String landscapeId, String versionId, String draftId) {
        return delete(
                landscapeId, versionId, draftId, DraftDeleteRequest.builder().build());
    }

    public IcePanelClientHttpResponse<Map<String, Object>> delete(
            String landscapeId, String versionId, String draftId, DraftDeleteRequest request) {
        return delete(landscapeId, versionId, draftId, request, null);
    }

    public IcePanelClientHttpResponse<Map<String, Object>> delete(
            String landscapeId,
            String versionId,
            String draftId,
            DraftDeleteRequest request,
            RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("landscapes")
                .addPathSegment(landscapeId)
                .addPathSegments("versions")
                .addPathSegment(versionId)
                .addPathSegments("drafts")
                .addPathSegment(draftId)
                .build();
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new IcePanelClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, new TypeReference<Map<String, Object>>() {}),
                        response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new IcePanelClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new IcePanelClientException("Network error executing HTTP request", e);
        }
    }

    public IcePanelClientHttpResponse<DraftsUpdateResponse> update(
            String landscapeId, String versionId, String draftId, DraftUpdateRequest request) {
        return update(landscapeId, versionId, draftId, request, null);
    }

    public IcePanelClientHttpResponse<DraftsUpdateResponse> update(
            String landscapeId,
            String versionId,
            String draftId,
            DraftUpdateRequest request,
            RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("landscapes")
                .addPathSegment(landscapeId)
                .addPathSegments("versions")
                .addPathSegment(versionId)
                .addPathSegments("drafts")
                .addPathSegment(draftId)
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new IcePanelClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new IcePanelClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DraftsUpdateResponse.class), response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new IcePanelClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new IcePanelClientException("Network error executing HTTP request", e);
        }
    }

    public IcePanelClientHttpResponse<DraftsMergeResponse> merge(String landscapeId, String versionId, String draftId) {
        return merge(
                landscapeId, versionId, draftId, DraftMergeRequest.builder().build());
    }

    public IcePanelClientHttpResponse<DraftsMergeResponse> merge(
            String landscapeId, String versionId, String draftId, DraftMergeRequest request) {
        return merge(landscapeId, versionId, draftId, request, null);
    }

    public IcePanelClientHttpResponse<DraftsMergeResponse> merge(
            String landscapeId,
            String versionId,
            String draftId,
            DraftMergeRequest request,
            RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("landscapes")
                .addPathSegment(landscapeId)
                .addPathSegments("versions")
                .addPathSegment(versionId)
                .addPathSegments("drafts")
                .addPathSegment(draftId)
                .addPathSegments("merge")
                .build();
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new IcePanelClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DraftsMergeResponse.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new IcePanelClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new IcePanelClientException("Network error executing HTTP request", e);
        }
    }
}
