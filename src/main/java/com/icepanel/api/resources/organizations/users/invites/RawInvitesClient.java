/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.icepanel.api.resources.organizations.users.invites;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.icepanel.api.core.ClientOptions;
import com.icepanel.api.core.IcePanelClientApiException;
import com.icepanel.api.core.IcePanelClientException;
import com.icepanel.api.core.IcePanelClientHttpResponse;
import com.icepanel.api.core.MediaTypes;
import com.icepanel.api.core.ObjectMappers;
import com.icepanel.api.core.RequestOptions;
import com.icepanel.api.errors.ConflictError;
import com.icepanel.api.errors.InternalServerError;
import com.icepanel.api.errors.NotFoundError;
import com.icepanel.api.errors.TooManyRequestsError;
import com.icepanel.api.errors.UnauthorizedError;
import com.icepanel.api.errors.UnprocessableEntityError;
import com.icepanel.api.resources.organizations.users.invites.requests.OrganizationUserInviteCreateRequest;
import com.icepanel.api.resources.organizations.users.invites.requests.OrganizationUserInviteRevokeRequest;
import com.icepanel.api.resources.organizations.users.invites.requests.OrganizationUserInvitesListRequest;
import com.icepanel.api.resources.organizations.users.invites.types.InvitesCreateResponse;
import com.icepanel.api.resources.organizations.users.invites.types.InvitesListResponse;
import com.icepanel.api.resources.organizations.users.invites.types.InvitesRevokeResponse;
import com.icepanel.api.types.Error;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawInvitesClient {
    protected final ClientOptions clientOptions;

    public RawInvitesClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public IcePanelClientHttpResponse<InvitesListResponse> list(String organizationId) {
        return list(organizationId, OrganizationUserInvitesListRequest.builder().build());
    }

    public IcePanelClientHttpResponse<InvitesListResponse> list(
            String organizationId, OrganizationUserInvitesListRequest request) {
        return list(organizationId, request, null);
    }

    public IcePanelClientHttpResponse<InvitesListResponse> list(
            String organizationId, OrganizationUserInvitesListRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("organizations")
                .addPathSegment(organizationId)
                .addPathSegments("users")
                .addPathSegments("invites")
                .build();
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new IcePanelClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, InvitesListResponse.class), response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new IcePanelClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new IcePanelClientException("Network error executing HTTP request", e);
        }
    }

    public IcePanelClientHttpResponse<InvitesCreateResponse> create(
            String organizationId, OrganizationUserInviteCreateRequest request) {
        return create(organizationId, request, null);
    }

    public IcePanelClientHttpResponse<InvitesCreateResponse> create(
            String organizationId, OrganizationUserInviteCreateRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("organizations")
                .addPathSegment(organizationId)
                .addPathSegments("users")
                .addPathSegments("invites")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new IcePanelClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new IcePanelClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, InvitesCreateResponse.class), response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new IcePanelClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new IcePanelClientException("Network error executing HTTP request", e);
        }
    }

    public IcePanelClientHttpResponse<InvitesRevokeResponse> revoke(
            String organizationId, String organizationUserInviteId) {
        return revoke(
                organizationId,
                organizationUserInviteId,
                OrganizationUserInviteRevokeRequest.builder().build());
    }

    public IcePanelClientHttpResponse<InvitesRevokeResponse> revoke(
            String organizationId, String organizationUserInviteId, OrganizationUserInviteRevokeRequest request) {
        return revoke(organizationId, organizationUserInviteId, request, null);
    }

    public IcePanelClientHttpResponse<InvitesRevokeResponse> revoke(
            String organizationId,
            String organizationUserInviteId,
            OrganizationUserInviteRevokeRequest request,
            RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("organizations")
                .addPathSegment(organizationId)
                .addPathSegments("users/invites")
                .addPathSegment(organizationUserInviteId)
                .build();
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new IcePanelClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, InvitesRevokeResponse.class), response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new IcePanelClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new IcePanelClientException("Network error executing HTTP request", e);
        }
    }
}
