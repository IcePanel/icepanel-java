/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.icepanel.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class DiagramStatus {
    public static final DiagramStatus CURRENT = new DiagramStatus(Value.CURRENT, "current");

    private final Value value;

    private final String string;

    DiagramStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof DiagramStatus && this.string.equals(((DiagramStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case CURRENT:
                return visitor.visitCurrent();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static DiagramStatus valueOf(String value) {
        switch (value) {
            case "current":
                return CURRENT;
            default:
                return new DiagramStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        CURRENT,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitCurrent();

        T visitUnknown(String unknownType);
    }
}
