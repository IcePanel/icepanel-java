/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.icepanel.organizations.logs;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.icepanel.core.ClientOptions;
import com.icepanel.core.IcePanelClientApiException;
import com.icepanel.core.IcePanelClientException;
import com.icepanel.core.IcePanelClientHttpResponse;
import com.icepanel.core.ObjectMappers;
import com.icepanel.core.QueryStringMapper;
import com.icepanel.core.RequestOptions;
import com.icepanel.errors.ForbiddenError;
import com.icepanel.errors.InternalServerError;
import com.icepanel.errors.NotFoundError;
import com.icepanel.errors.UnauthorizedError;
import com.icepanel.errors.UnprocessableEntityError;
import com.icepanel.organizations.logs.types.OrganizationLogStatsByEntityRequest;
import com.icepanel.organizations.logs.types.OrganizationLogStatsByTypeRequest;
import com.icepanel.types.ActionLogStatsListByEntity;
import com.icepanel.types.Error;
import com.icepanel.types.OrganizationLogStatsListByType;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawStatsClient {
    protected final ClientOptions clientOptions;

    public RawStatsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * List total counts of actions for each action type (e.g. diagram-content-update, model-object-create)
     */
    public IcePanelClientHttpResponse<OrganizationLogStatsListByType> byType(
            OrganizationLogStatsByTypeRequest request) {
        return byType(request, null);
    }

    /**
     * List total counts of actions for each action type (e.g. diagram-content-update, model-object-create)
     */
    public IcePanelClientHttpResponse<OrganizationLogStatsListByType> byType(
            OrganizationLogStatsByTypeRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("organizations")
                .addPathSegment(request.getOrganizationId())
                .addPathSegments("logs/stats")
                .addPathSegments("by-type");
        if (request.getFilter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter", request.getFilter().get(), false);
        }
        if (request.getPeriod().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "period", request.getPeriod().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new IcePanelClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, OrganizationLogStatsListByType.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new IcePanelClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new IcePanelClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * List total counts of organizations for each entity identifier
     */
    public IcePanelClientHttpResponse<ActionLogStatsListByEntity> byEntity(
            OrganizationLogStatsByEntityRequest request) {
        return byEntity(request, null);
    }

    /**
     * List total counts of organizations for each entity identifier
     */
    public IcePanelClientHttpResponse<ActionLogStatsListByEntity> byEntity(
            OrganizationLogStatsByEntityRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("organizations")
                .addPathSegment(request.getOrganizationId())
                .addPathSegments("logs/stats")
                .addPathSegments("by-entity");
        if (request.getFilter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter", request.getFilter().get(), false);
        }
        if (request.getPeriod().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "period", request.getPeriod().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new IcePanelClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ActionLogStatsListByEntity.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new IcePanelClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new IcePanelClientException("Network error executing HTTP request", e);
        }
    }
}
