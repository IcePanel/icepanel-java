/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.icepanel.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class DraftStatus {
    public static final DraftStatus IN_PROGRESS = new DraftStatus(Value.IN_PROGRESS, "in-progress");

    public static final DraftStatus MERGED = new DraftStatus(Value.MERGED, "merged");

    public static final DraftStatus ARCHIVED = new DraftStatus(Value.ARCHIVED, "archived");

    private final Value value;

    private final String string;

    DraftStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other) || (other instanceof DraftStatus && this.string.equals(((DraftStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case IN_PROGRESS:
                return visitor.visitInProgress();
            case MERGED:
                return visitor.visitMerged();
            case ARCHIVED:
                return visitor.visitArchived();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static DraftStatus valueOf(String value) {
        switch (value) {
            case "in-progress":
                return IN_PROGRESS;
            case "merged":
                return MERGED;
            case "archived":
                return ARCHIVED;
            default:
                return new DraftStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        IN_PROGRESS,

        MERGED,

        ARCHIVED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitInProgress();

        T visitMerged();

        T visitArchived();

        T visitUnknown(String unknownType);
    }
}
